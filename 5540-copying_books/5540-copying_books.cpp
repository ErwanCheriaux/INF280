#include <stdio.h>
#include <stdlib.h>
#include <cstring>
#include <iostream>
#include <cstdint>
#include <algorithm>

using namespace std;

int main(void)
{
   int N;
   scanf("%d", &N);

   for(int t=0; t<N; t++)
   {
      //lecture input
      int m, k;
      scanf("%d %d", &m, &k);

      int p[m];

      for(int i=0; i<m; i++)
      {
         scanf("%d", &p[i]);
      }

      //algo

   }
   return 0;
}
