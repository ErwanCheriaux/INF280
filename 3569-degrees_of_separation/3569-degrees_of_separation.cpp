#include <stdio.h>
#include <stdlib.h>
#include <cstring>
#include <iostream>
#include <cstdint>
#include <algorithm>

using namespace std;

int main(void)
{
   while(1)
   {
      int P, R;

      scanf("%d %d", &P, &R);
      if(!P and !R) return 0;

      for(int i=0; i<R; i++)
      {
         string str1, str2;
         cin >> str1 >> str2;
      }
   }
}
